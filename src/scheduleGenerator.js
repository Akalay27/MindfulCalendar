import {Activities} from 'data/activities'
import {Moment} from 'moment'


export class scheduleGenerator {

    // constructor - takes in activity Manager
    


    /*
        generateSchedule(startTime, endTime, lunchTime) {
            
            st = startTime.copy()
            maxDur = 90
            lwb = 0;
            while (st < endTime) {

                for each (name, duration) in durations: 
                    if (duration <= 0) 
                        continue;
                    amg.addActivity(startTime,startTime + maxDur, duration.name, work)
                    startTime += maxDur;
                    duration -= maxDur;
                    lwb -= duration;

                ADD LEISURE ACTIVITIES
                
                lwb ++;

            }

            


        }






    */


}